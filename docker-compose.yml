services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: save
    ports:
      - "5432:5432"
  api:
    build: .
    env_file: .env
    depends_on: [db]
    ports:
      - "8000:8000"
